import { StyleSheet } from 'react-native';

const containerWhiteStyles = StyleSheet.create({
     container: {
       flex: 1,
       backgroundColor: '#faf9f7',
       margin: 0,
       padding: 0,
         color: '#143635',
     },
   });

//if text in a view which is in a different color: #faf9f7
//call to action: #e3c2eb

export default containerWhiteStyles;