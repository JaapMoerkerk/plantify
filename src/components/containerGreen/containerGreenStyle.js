import { StyleSheet } from 'react-native';

const containerGreenStyle = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: '#9ed267',
        margin: 0,
        padding: 0,
        color: '#faf9f7',
    },
});

// call to action: #d5e9bd

export default containerGreenStyle;